ID: SAPPRESS
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: EPM_MODEL
   type: hdb
   path: EPM_MODEL
   requires:
    - name: hdi_EPM_MODEL

resources:
 - name: hdi_EPM_MODEL
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
